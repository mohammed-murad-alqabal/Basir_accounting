# سجل معالجة تاريخ الأسرار

## الملخص

سجل فحص الأسرار في بوابة جودة الإنتاج اكتشافات تاريخية محجوبة ضمن الملف التالي فقط:

```text
tools/git-merge-strategy/temp/security_scan_raw.txt
```

لا يحتوي الشجر الحالي لفرع طلب الدمج على هذا الملف. وقد أثبت جرد المراجع أن الالتزام المتأثر لا يدخل في سجل فرع `chore/dependency-security-monitoring` ولا في سجل PR #15؛ المرجع البعيد الوحيد الذي أبقاه قابلاً للوصول هو الوسم التاريخي:

```text
comprehensive-analysis-complete-20251222_200353
```

## قرار المعالجة

سيُحذف الوسم المذكور من المصدر البعيد والمحلي. وبذلك تصبح الكتلة التاريخية المتأثرة غير قابلة للوصول عبر أي فرع أو وسم معروض من المستودع. لا تُعاد كتابة فروع مستقلة لا تضم الالتزام المتأثر؛ فهذا يقلل مساحة المخاطرة ويحافظ على سلامة عمل المتعاونين.

> لا يحل حذف المرجع محل إبطال بيانات الاعتماد المحتمل تعرضها. يجب على مالكي أي اعتماد ظهر في الاكتشافات التاريخية اعتباره مكشوفاً وإبطاله واستبداله من مزود الخدمة المعني، من دون إدراج قيمة الاعتماد في المستودع أو في سجل المعالجة.

## خطوات التحقق

بعد حذف المرجع، يُجرى فحص `git rev-list --all` للتحقق من أن الالتزام المتأثر غير قابل للوصول، ثم يُشغّل فحص Gitleaks التاريخي محلياً وبوابة الإنتاج على GitHub. لا يصبح PR #15 مؤهلاً للمراجعة البشرية إلا بعد اجتياز وظيفة مسح الأسرار والبوابة المركبة.

## أثر العمل على المتعاونين

الوسم المحذوف مرجع تاريخي فقط، ولا يغير فروع التطوير أو طلب الدمج. يجب على أي نسخة محلية تحتوي الوسم تحديث مراجعها وحذف الوسم المحلي، ثم تشغيل `git fetch --prune --tags` قبل الاعتماد على الجرد الأمني الجديد.
