[1mdiff --git a/rust/crates/accounting_core/src/ledger/validation.rs b/rust/crates/accounting_core/src/ledger/validation.rs[m
[1mindex 3ee23937..859a4c50 100644[m
[1m--- a/rust/crates/accounting_core/src/ledger/validation.rs[m
[1m+++ b/rust/crates/accounting_core/src/ledger/validation.rs[m
[36m@@ -52,11 +52,84 @@[m [mpub enum EntryValidationError {[m
     /// Missing partner for sub-ledger account (CP-010 violation)[m
     #[error("Missing partner for sub-ledger account: {0}")][m
     MissingPartner(String),[m
[32m+[m
[32m+[m[32m    /// Journal line references an account that is not present in the chart of accounts.[m
[32m+[m[32m    #[error("Account not found: {0}")][m
[32m+[m[32m    AccountNotFound(uuid::Uuid),[m
[32m+[m
[32m+[m[32m    /// Journal line references an account that is not active.[m
[32m+[m[32m    #[error("Account is inactive: {0}")][m
[32m+[m[32m    InactiveAccount(String),[m
[32m+[m
[32m+[m[32m    /// Journal line references a non-postable parent account.[m
[32m+[m[32m    #[error("Account is not postable because it is not a leaf: {0}")][m
[32m+[m[32m    NonPostableAccount(String),[m
[32m+[m
[32m+[m[32m    /// A journal line has an invalid debit/credit shape or a negative amount.[m
[32m+[m[32m    #[error("Invalid line amount: {0}")][m
[32m+[m[32m    InvalidLineAmount(String),[m
[32m+[m
[32m+[m[32m    /// A journal line has both debit and credit amounts.[m
[32m+[m[32m    #[error("Line has both debit and credit amounts: {0}")][m
[32m+[m[32m    AmbiguousLineAmount(String),[m
 }[m
 [m
 /// Result type for entry validation.[m
 pub type ValidationResult<T> = Result<T, EntryValidationError>;[m
 [m
[32m+[m[32m/// Validate that every line has exactly one positive side.[m
[32m+[m[32mpub fn validate_line_amounts(entry: &JournalEntry) -> ValidationResult<()> {[m
[32m+[m[32m    for (i, line) in entry.lines.iter().enumerate() {[m
[32m+[m[32m        if line.debit_amount < Decimal::ZERO || line.credit_amount < Decimal::ZERO {[m
[32m+[m[32m            return Err(EntryValidationError::InvalidLineAmount(format!([m
[32m+[m[32m                "Line {} contains a negative debit or credit",[m
[32m+[m[32m                i + 1[m
[32m+[m[32m            )));[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        let has_debit = line.debit_amount > Decimal::ZERO;[m
[32m+[m[32m        let has_credit = line.credit_amount > Decimal::ZERO;[m
[32m+[m[32m        match (has_debit, has_credit) {[m
[32m+[m[32m            (true, true) => {[m
[32m+[m[32m                return Err(EntryValidationError::AmbiguousLineAmount(format!([m
[32m+[m[32m                    "line {}",[m
[32m+[m[32m                    i + 1[m
[32m+[m[32m                )))[m
[32m+[m[32m            }[m
[32m+[m[32m            (false, false) => {[m
[32m+[m[32m                return Err(EntryValidationError::InvalidLineAmount(format!([m
[32m+[m[32m                    "Line {} must contain a positive debit or credit",[m
[32m+[m[32m                    i + 1[m
[32m+[m[32m                )))[m
[32m+[m[32m            }[m
[32m+[m[32m            _ => {}[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
[32m+[m[32m    Ok(())[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m/// Validate that every line targets an active, postable account.[m
[32m+[m[32mpub fn validate_accounts([m
[32m+[m[32m    entry: &JournalEntry,[m
[32m+[m[32m    accounts: &crate::accounts::AccountRegistry,[m
[32m+[m[32m) -> ValidationResult<()> {[m
[32m+[m[32m    for line in &entry.lines {[m
[32m+[m[32m        let account = accounts[m
[32m+[m[32m            .get(&line.account_id)[m
[32m+[m[32m            .ok_or(EntryValidationError::AccountNotFound(line.account_id))?;[m
[32m+[m
[32m+[m[32m        if !account.is_active {[m
[32m+[m[32m            return Err(EntryValidationError::InactiveAccount(account.code.clone()));[m
[32m+[m[32m        }[m
[32m+[m[32m        if !accounts.is_leaf(&account.id) {[m
[32m+[m[32m            return Err(EntryValidationError::NonPostableAccount([m
[32m+[m[32m                account.code.clone(),[m
[32m+[m[32m            ));[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
[32m+[m[32m    Ok(())[m
[32m+[m[32m}[m
[32m+[m
 /// Validate that an entry is balanced (CP-001).[m
 ///[m
 /// This is the most critical validation - the fundamental accounting equation.[m
[36m@@ -163,16 +236,27 @@[m [mpub fn validate_traceability(entry: &JournalEntry) -> ValidationResult<()> {[m
 /// Original Amount * Exchange Rate == (Debit or Credit Amount)[m
 pub fn validate_currency(entry: &JournalEntry) -> ValidationResult<()> {[m
     for (i, line) in entry.lines.iter().enumerate() {[m
[32m+[m[32m        let supplied = [[m
[32m+[m[32m            line.original_currency.is_some(),[m
[32m+[m[32m            line.exchange_rate.is_some(),[m
[32m+[m[32m            line.original_amount.is_some(),[m
[32m+[m[32m        ];[m
[32m+[m[32m        if supplied.iter().any(|present| *present) && supplied.iter().any(|present| !*present) {[m
[32m+[m[32m            return Err(EntryValidationError::CurrencyMismatch(format!([m
[32m+[m[32m                "Line {} must provide original currency, amount, and exchange rate together",[m
[32m+[m[32m                i + 1[m
[32m+[m[32m            )));[m
[32m+[m[32m        }[m
[32m+[m
         if let (Some(orig_curr), Some(rate), Some(orig_amt)) = ([m
             &line.original_currency,[m
             line.exchange_rate,[m
             line.original_amount,[m
         ) {[m
[31m-            if rate <= Decimal::ZERO {[m
[32m+[m[32m            if rate <= Decimal::ZERO || orig_amt <= Decimal::ZERO {[m
                 return Err(EntryValidationError::CurrencyMismatch(format!([m
[31m-                    "Line {}: Exchange rate must be positive, found {}",[m
[31m-                    i + 1,[m
[31m-                    rate[m
[32m+[m[32m                    "Line {}: Original amount and exchange rate must be positive",[m
[32m+[m[32m                    i + 1[m
                 )));[m
             }[m
 [m
[36m@@ -226,12 +310,14 @@[m [mpub fn validate_for_posting([m
     accounts: &crate::accounts::AccountRegistry,[m
 ) -> ValidationResult<()> {[m
     validate_has_lines(entry)?;[m
[32m+[m[32m    validate_line_amounts(entry)?;[m
     validate_balance(entry)?;[m
     validate_standards_format(entry)?;[m
     validate_standards_exists(entry, registry)?;[m
     validate_temporal(entry)?;[m
     validate_traceability(entry)?;[m
     validate_currency(entry)?;[m
[32m+[m[32m    validate_accounts(entry, accounts)?;[m
     validate_partners(entry, accounts)?;[m
     Ok(())[m
 }[m
[36m@@ -358,11 +444,83 @@[m [mmod tests {[m
     #[test][m
     fn test_validate_for_posting_success() {[m
         let registry = StandardsRegistry::load_defaults();[m
[31m-        let accounts = crate::accounts::AccountRegistry::new(vec![]);[m
         let entry = make_entry(Decimal::from(500), Decimal::from(500), "IFRS 15.35");[m
[32m+[m[32m        let mut debit_account = crate::accounts::models::Account::new([m
[32m+[m[32m            "1000",[m
[32m+[m[32m            "النقدية",[m
[32m+[m[32m            "Cash",[m
[32m+[m[32m            crate::accounts::models::AccountKind::Asset,[m
[32m+[m[32m        );[m
[32m+[m[32m        debit_account.id = entry.lines[0].account_id;[m
[32m+[m[32m        let mut credit_account = crate::accounts::models::Account::new([m
[32m+[m[32m            "4000",[m
[32m+[m[32m            "الإيراد",[m
[32m+[m[32m            "Revenue",[m
[32m+[m[32m            crate::accounts::models::AccountKind::Income,[m
[32m+[m[32m        );[m
[32m+[m[32m        credit_account.id = entry.lines[1].account_id;[m
[32m+[m[32m        let accounts = crate::accounts::AccountRegistry::new(vec![debit_account, credit_account]);[m
         assert!(validate_for_posting(&entry, &registry, &accounts).is_ok());[m
     }[m
 [m
[32m+[m[32m    #[test][m
[32m+[m[32m    fn test_validate_line_amounts_rejects_negative_and_ambiguous_lines() {[m
[32m+[m[32m        let mut entry = make_entry(Decimal::from(500), Decimal::from(500), "IFRS 15.35");[m
[32m+[m[32m        entry.lines[0].debit_amount = Decimal::from(-1);[m
[32m+[m[32m        assert!(matches!([m
[32m+[m[32m            validate_line_amounts(&entry),[m
[32m+[m[32m            Err(EntryValidationError::InvalidLineAmount(_))[m
[32m+[m[32m        ));[m
[32m+[m
[32m+[m[32m        entry.lines[0].debit_amount = Decimal::from(500);[m
[32m+[m[32m        entry.lines[0].credit_amount = Decimal::from(1);[m
[32m+[m[32m        assert!(matches!([m
[32m+[m[32m            validate_line_amounts(&entry),[m
[32m+[m[32m            Err(EntryValidationError::AmbiguousLineAmount(_))[m
[32m+[m[32m        ));[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    #[test][m
[32m+[m[32m    fn test_validate_accounts_rejects_unknown_and_non_leaf_accounts() {[m
[32m+[m[32m        let entry = make_entry(Decimal::from(500), Decimal::from(500), "IFRS 15.35");[m
[32m+[m[32m        let accounts = crate::accounts::AccountRegistry::new(vec![]);[m
[32m+[m[32m        assert!(matches!([m
[32m+[m[32m            validate_accounts(&entry, &accounts),[m
[32m+[m[32m            Err(EntryValidationError::AccountNotFound(_))[m
[32m+[m[32m        ));[m
[32m+[m
[32m+[m[32m        let mut parent = crate::accounts::models::Account::new([m
[32m+[m[32m            "1000",[m
[32m+[m[32m            "الأصول",[m
[32m+[m[32m            "Assets",[m
[32m+[m[32m            crate::accounts::models::AccountKind::Asset,[m
[32m+[m[32m        );[m
[32m+[m[32m        parent.id = entry.lines[0].account_id;[m
[32m+[m[32m        let mut child = crate::accounts::models::Account::new([m
[32m+[m[32m            "1100",[m
[32m+[m[32m            "النقدية",[m
[32m+[m[32m            "Cash",[m
[32m+[m[32m            crate::accounts::models::AccountKind::Asset,[m
[32m+[m[32m        );[m
[32m+[m[32m        child.id = uuid::Uuid::new_v4();[m
[32m+[m[32m        child.parent_id = Some(parent.id);[m
[32m+[m[32m        let accounts = crate::accounts::AccountRegistry::new(vec![parent, child]);[m
[32m+[m[32m        assert!(matches!([m
[32m+[m[32m            validate_accounts(&entry, &accounts),[m
[32m+[m[32m            Err(EntryValidationError::NonPostableAccount(_))[m
[32m+[m[32m        ));[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    #[test][m
[32m+[m[32m    fn test_validate_currency_rejects_partial_metadata() {[m
[32m+[m[32m        let mut entry = make_entry(Decimal::from(500), Decimal::from(500), "IFRS 15.35");[m
[32m+[m[32m        entry.lines[0].original_currency = Some("USD".to_string());[m
[32m+[m[32m        assert!(matches!([m
[32m+[m[32m            validate_currency(&entry),[m
[32m+[m[32m            Err(EntryValidationError::CurrencyMismatch(_))[m
[32m+[m[32m        ));[m
[32m+[m[32m    }[m
[32m+[m
     #[test][m
     fn test_validate_for_posting_bad_reference() {[m
         let registry = StandardsRegistry::load_defaults();[m
[1mdiff --git a/rust/crates/accounting_data/src/db/ledger.rs b/rust/crates/accounting_data/src/db/ledger.rs[m
[1mindex 23608c9b..3d9f5404 100644[m
[1m--- a/rust/crates/accounting_data/src/db/ledger.rs[m
[1m+++ b/rust/crates/accounting_data/src/db/ledger.rs[m
[36m@@ -1,6 +1,9 @@[m
 use accounting_core::audit::chain::{compute_record_hash, GENESIS_HASH};[m
 use accounting_core::audit::models::{AuditAction, AuditMetadata, AuditRecord, WhatInfo};[m
 use accounting_core::ledger::models::{EntryStatus, JournalEntry};[m
[32m+[m[32muse accounting_core::ledger::validation::{[m
[32m+[m[32m    validate_balance, validate_currency, validate_has_lines, validate_line_amounts,[m
[32m+[m[32m};[m
 use chrono::Utc;[m
 use rust_decimal::Decimal;[m
 use sqlx::PgPool;[m
[36m@@ -47,6 +50,17 @@[m [mimpl PgLedgerRepository {[m
         entry: &JournalEntry,[m
         metadata: &AuditMetadata,[m
     ) -> Result<(), anyhow::Error> {[m
[32m+[m[32m        // Repository posting is the final boundary for all business flows. The[m
[32m+[m[32m        // native API and sub-ledgers can call this method directly, so enforce[m
[32m+[m[32m        // the database-independent invariants here as well as in the core service.[m
[32m+[m[32m        if entry.status != EntryStatus::Posted {[m
[32m+[m[32m            anyhow::bail!("Only entries in Posted status may be persisted to the ledger");[m
[32m+[m[32m        }[m
[32m+[m[32m        validate_has_lines(entry).map_err(|error| anyhow::anyhow!(error.to_string()))?;[m
[32m+[m[32m        validate_line_amounts(entry).map_err(|error| anyhow::anyhow!(error.to_string()))?;[m
[32m+[m[32m        validate_balance(entry).map_err(|error| anyhow::anyhow!(error.to_string()))?;[m
[32m+[m[32m        validate_currency(entry).map_err(|error| anyhow::anyhow!(error.to_string()))?;[m
[32m+[m
         // 1. Insert Header[m
         let adjustment_reason_str = entry.adjustment_reason.map(|r| format!("{:?}", r));[m
 [m
[1mdiff --git a/rust/crates/accounting_data/src/db/reporting.rs b/rust/crates/accounting_data/src/db/reporting.rs[m
[1mindex d68cb6a7..286bf984 100644[m
[1m--- a/rust/crates/accounting_data/src/db/reporting.rs[m
[1m+++ b/rust/crates/accounting_data/src/db/reporting.rs[m
[36m@@ -11,7 +11,7 @@[m [mpub struct PgReportingRepository {[m
 }[m
 [m
 /// Raw row from the database aggregation query.[m
[31m-#[derive(Debug)][m
[32m+[m[32m#[derive(Debug, sqlx::FromRow)][m
 struct BalanceRow {[m
     account_id: Uuid,[m
     account_code: String,[m
[36m@@ -23,6 +23,25 @@[m [mstruct BalanceRow {[m
     total_credits: Decimal,[m
 }[m
 [m
[32m+[m[32m#[derive(Debug, sqlx::FromRow)][m
[32m+[m[32mstruct AccountStatementRow {[m
[32m+[m[32m    id: Uuid,[m
[32m+[m[32m    code: String,[m
[32m+[m[32m    account_name_ar: String,[m
[32m+[m[32m    account_name_en: String,[m
[32m+[m[32m    account_type: String,[m
[32m+[m[32m    account_classification: Option<String>,[m
[32m+[m[32m    total_debits: Decimal,[m
[32m+[m[32m    total_credits: Decimal,[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m#[derive(Debug, sqlx::FromRow)][m
[32m+[m[32mstruct AccountBalanceSummaryRow {[m
[32m+[m[32m    id: Uuid,[m
[32m+[m[32m    total_debits: Decimal,[m
[32m+[m[32m    total_credits: Decimal,[m
[32m+[m[32m}[m
[32m+[m
 impl PgReportingRepository {[m
     pub fn new(pool: PgPool) -> Self {[m
         Self { pool }[m
[36m@@ -42,8 +61,7 @@[m [mimpl PgReportingRepository {[m
         // We use a Recursive CTE to aggregate balances from leaf accounts up to all their parents[m
         // This ensures the Trial Balance reflects the hierarchy correctly[m
         let rows = if let Some(start) = period_start {[m
[31m-            sqlx::query_as!([m
[31m-                BalanceRow,[m
[32m+[m[32m            sqlx::query_as::<_, BalanceRow>([m
                 r#"[m
                 WITH RECURSIVE account_tree AS ([m
                     -- Base case: Leaf accounts and their balances in the period[m
[36m@@ -56,6 +74,7 @@[m [mimpl PgReportingRepository {[m
                     LEFT JOIN journal_entry_lines l ON a.id = l.account_id[m
                     LEFT JOIN journal_entries e ON l.entry_id = e.id[m
                     WHERE a.is_leaf = true[m
[32m+[m[32m                      AND e.status = 'Posted'[m
                       AND e.effective_date >= $1 AND e.effective_date <= $2[m
                     GROUP BY a.id, a.parent_id[m
                     [m
[36m@@ -83,14 +102,13 @@[m [mimpl PgReportingRepository {[m
                 GROUP BY t.id, a.code, a.name_ar, a.name_en, a.type[m
                 ORDER BY a.code[m
                 "#,[m
[31m-                start,[m
[31m-                as_of_date[m
             )[m
[32m+[m[32m            .bind(start)[m
[32m+[m[32m            .bind(as_of_date)[m
             .fetch_all(&self.pool)[m
             .await?[m
         } else {[m
[31m-            sqlx::query_as!([m
[31m-                BalanceRow,[m
[32m+[m[32m            sqlx::query_as::<_, BalanceRow>([m
                 r#"[m
                 WITH RECURSIVE account_tree AS ([m
                     -- Base case: Leaf accounts and their total balances up to date[m
[36m@@ -103,7 +121,7 @@[m [mimpl PgReportingRepository {[m
                     LEFT JOIN journal_entry_lines l ON a.id = l.account_id[m
                     LEFT JOIN journal_entries e ON l.entry_id = e.id[m
                     WHERE a.is_leaf = true[m
[31m-                      AND (e.effective_date IS NULL OR e.effective_date <= $1)[m
[32m+[m[32m                      AND (e.status = 'Posted' AND e.effective_date <= $1)[m
                     GROUP BY a.id, a.parent_id[m
                     [m
                     UNION ALL[m
[36m@@ -130,8 +148,8 @@[m [mimpl PgReportingRepository {[m
                 GROUP BY t.id, a.code, a.name_ar, a.name_en, a.type[m
                 ORDER BY a.code[m
                 "#,[m
[31m-                as_of_date[m
             )[m
[32m+[m[32m            .bind(as_of_date)[m
             .fetch_all(&self.pool)[m
             .await?[m
         };[m
[36m@@ -196,8 +214,7 @@[m [mimpl PgReportingRepository {[m
         period_end: NaiveDate,[m
     ) -> Result<Vec<DrillDownEntry>, anyhow::Error> {[m
         let entries = if let Some(start) = period_start {[m
[31m-            sqlx::query_as!([m
[31m-                DrillDownEntry,[m
[32m+[m[32m            sqlx::query_as::<_, DrillDownEntry>([m
                 r#"[m
                 SELECT [m
                     e.id as entry_id,[m
[36m@@ -210,19 +227,19 @@[m [mimpl PgReportingRepository {[m
                 FROM journal_entries e[m
                 JOIN journal_entry_lines l ON e.id = l.entry_id[m
                 WHERE l.account_id = $1[m
[32m+[m[32m                  AND e.status = 'Posted'[m
                   AND e.effective_date >= $2[m
                   AND e.effective_date <= $3[m
                 ORDER BY e.effective_date, e.entry_number[m
                 "#,[m
[31m-                account_id,[m
[31m-                start,[m
[31m-                period_end[m
             )[m
[32m+[m[32m            .bind(account_id)[m
[32m+[m[32m            .bind(start)[m
[32m+[m[32m            .bind(period_end)[m
             .fetch_all(&self.pool)[m
             .await?[m
         } else {[m
[31m-            sqlx::query_as!([m
[31m-                DrillDownEntry,[m
[32m+[m[32m            sqlx::query_as::<_, DrillDownEntry>([m
                 r#"[m
                 SELECT [m
                     e.id as entry_id,[m
[36m@@ -235,12 +252,13 @@[m [mimpl PgReportingRepository {[m
                 FROM journal_entries e[m
                 JOIN journal_entry_lines l ON e.id = l.entry_id[m
                 WHERE l.account_id = $1[m
[32m+[m[32m                  AND e.status = 'Posted'[m
                   AND e.effective_date <= $2[m
                 ORDER BY e.effective_date, e.entry_number[m
                 "#,[m
[31m-                account_id,[m
[31m-                period_end[m
             )[m
[32m+[m[32m            .bind(account_id)[m
[32m+[m[32m            .bind(period_end)[m
             .fetch_all(&self.pool)[m
             .await?[m
         };[m
[36m@@ -254,29 +272,30 @@[m [mimpl PgReportingRepository {[m
         from_date: NaiveDate,[m
         to_date: NaiveDate,[m
     ) -> Result<FinancialReport, anyhow::Error> {[m
[31m-        let rows = sqlx::query!([m
[32m+[m[32m        let rows = sqlx::query_as::<_, AccountStatementRow>([m
             r#"[m
             SELECT [m
                 a.id,[m
                 a.code,[m
                 a.name_ar as account_name_ar,[m
                 a.name_en as account_name_en,[m
[31m-                a.type as "account_type!",[m
[32m+[m[32m                a.type as account_type,[m
                 a.classification as account_classification,[m
[31m-                COALESCE(SUM(l.debit), 0) as "total_debits!: Decimal",[m
[31m-                COALESCE(SUM(l.credit), 0) as "total_credits!: Decimal"[m
[32m+[m[32m                COALESCE(SUM(l.debit), 0) as total_debits,[m
[32m+[m[32m                COALESCE(SUM(l.credit), 0) as total_credits[m
             FROM accounts a[m
             LEFT JOIN journal_entry_lines l ON a.id = l.account_id[m
             LEFT JOIN journal_entries e ON l.entry_id = e.id[m
             WHERE a.is_leaf = true[m
               AND (a.type = 'Income' OR a.type = 'Expense')[m
[32m+[m[32m              AND e.status = 'Posted'[m
               AND e.effective_date >= $1[m
               AND e.effective_date <= $2[m
             GROUP BY a.id, a.code, a.name_ar, a.name_en, a.type, a.classification[m
             "#,[m
[31m-            from_date,[m
[31m-            to_date[m
         )[m
[32m+[m[32m        .bind(from_date)[m
[32m+[m[32m        .bind(to_date)[m
         .fetch_all(&self.pool)[m
         .await?;[m
 [m
[36m@@ -342,27 +361,28 @@[m [mimpl PgReportingRepository {[m
         use accounting_core::accounts::models::{Account, AccountClassification, AccountKind};[m
         use accounting_core::reporting::generator::FinancialReportGenerator;[m
 [m
[31m-        let rows = sqlx::query!([m
[32m+[m[32m        let rows = sqlx::query_as::<_, AccountStatementRow>([m
             r#"[m
             SELECT [m
                 a.id,[m
                 a.code,[m
                 a.name_ar as account_name_ar,[m
                 a.name_en as account_name_en,[m
[31m-                a.type as "account_type!",[m
[32m+[m[32m                a.type as account_type,[m
                 a.classification as account_classification,[m
[31m-                COALESCE(SUM(l.debit), 0) as "total_debits!: Decimal",[m
[31m-                COALESCE(SUM(l.credit), 0) as "total_credits!: Decimal"[m
[32m+[m[32m                COALESCE(SUM(l.debit), 0) as total_debits,[m
[32m+[m[32m                COALESCE(SUM(l.credit), 0) as total_credits[m
             FROM accounts a[m
             LEFT JOIN journal_entry_lines l ON a.id = l.account_id[m
             LEFT JOIN journal_entries e ON l.entry_id = e.id[m
             WHERE a.is_leaf = true[m
               AND (a.type = 'Asset' OR a.type = 'Liability' OR a.type = 'Equity')[m
[32m+[m[32m              AND e.status = 'Posted'[m
               AND e.effective_date <= $1[m
             GROUP BY a.id, a.code, a.name_ar, a.name_en, a.type, a.classification[m
             "#,[m
[31m-            as_of_date[m
         )[m
[32m+[m[32m        .bind(as_of_date)[m
         .fetch_all(&self.pool)[m
         .await?;[m
 [m
[36m@@ -480,20 +500,20 @@[m [mimpl PgReportingRepository {[m
         &self,[m
         date: NaiveDate,[m
     ) -> Result<std::collections::HashMap<Uuid, Decimal>, anyhow::Error> {[m
[31m-        let rows = sqlx::query!([m
[32m+[m[32m        let rows = sqlx::query_as::<_, AccountBalanceSummaryRow>([m
             r#"[m
             SELECT [m
                 a.id,[m
[31m-                COALESCE(SUM(l.debit), 0) as "total_debits!: Decimal",[m
[31m-                COALESCE(SUM(l.credit), 0) as "total_credits!: Decimal"[m
[32m+[m[32m                COALESCE(SUM(l.debit), 0) as total_debits,[m
[32m+[m[32m                COALESCE(SUM(l.credit), 0) as total_credits[m
             FROM accounts a[m
             LEFT JOIN journal_entry_lines l ON a.id = l.account_id[m
             LEFT JOIN journal_entries e ON l.entry_id = e.id[m
[31m-            WHERE e.effective_date <= $1 OR e.effective_date IS NULL[m
[32m+[m[32m            WHERE e.status = 'Posted' AND e.effective_date <= $1[m
             GROUP BY a.id[m
             "#,[m
[31m-            date[m
         )[m
[32m+[m[32m        .bind(date)[m
         .fetch_all(&self.pool)[m
         .await?;[m
 [m
[1mdiff --git a/rust/crates/accounting_native/src/api/ledger.rs b/rust/crates/accounting_native/src/api/ledger.rs[m
[1mindex 292ad88e..8241a412 100644[m
[1m--- a/rust/crates/accounting_native/src/api/ledger.rs[m
[1m+++ b/rust/crates/accounting_native/src/api/ledger.rs[m
[36m@@ -84,13 +84,18 @@[m [mpub async fn post_journal_entry([m
     let pool = get_pool()?;[m
     let ledger_repo = PgLedgerRepository::new(pool.clone());[m
     let standards_repo = PgStandardsRepository::new(pool.clone());[m
[32m+[m[32m    let account_repo = PgAccountRepository::new(pool.clone());[m
 [m
[31m-    let _registry: Vec<accounting_core::standards::models::StandardEntry> =[m
[31m-        standards_repo.load_all().await?;[m
[32m+[m[32m    let standards = standards_repo.load_all().await?;[m
[32m+[m[32m    let registry = StandardsRegistry::from_entries(standards);[m
[32m+[m[32m    let accounts = account_repo.list_all().await?;[m
[32m+[m[32m    let account_registry = AccountRegistry::new(accounts);[m
     let mut entry = map_dto_to_entity(dto)?;[m
 [m
[31m-    // Set to posted before saving (or let repository handle it)[m
[32m+[m[32m    // Mark the entry as posted only after the complete canonical validation context[m
[32m+[m[32m    // has been loaded. The repository applies the same structural checks again.[m
     entry.status = EntryStatus::Posted;[m
[32m+[m[32m    validate_for_posting(&entry, &registry, &account_registry)?;[m
 [m
     let audit_meta: accounting_core::audit::models::AuditMetadata = metadata.try_into()?;[m
 [m
