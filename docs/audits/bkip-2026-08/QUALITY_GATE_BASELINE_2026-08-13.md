# BKIP — خط أساس بوابة تحليل الكود والاختبارات

> **status:** ARCHIVED
> **historical_as_of_sha:** `36ec0bc977a43abc6c4e0f12fe114a0d77158cb1`
> **historical_as_of_date:** 2026-08-13
> **not_current_source_of_truth:** true
> **superseded_by:** Baseline لاحق موثق عند تغيير قواعد التحليل أو بعد معالجة الدفعة التالية من الملاحظات

## الغرض والنطاق

يسجل هذا المستند **لقطة دليل قابلة لإعادة الإنتاج** لحالة بوابتي تحليل Flutter واختباراته عند SHA المحدد أعلاه. لا يمثل نجاح أي أمر فيه ادعاء جاهزية إنتاج أو امتثال أو اكتمال المشروع. الغرض منه هو ضبط التحول التدريجي المنصوص عليه في خارطة BKIP: تبقى العيوب الدلالية مانعة للدمج، بينما تعامل الملاحظات المعلوماتية المتراكمة كدين جودة مرئي واستشاري إلى أن تعالج على دفعات صغيرة مدعومة باختبارات.[1]

أجري الفحص على Flutter `3.35.5` وDart `3.9.2` بعد تنفيذ توليد المصادر التالي:

```bash
 dart run build_runner build --delete-conflicting-outputs
 flutter analyze --no-fatal-infos
 flutter test --coverage
```

## نتائج التحليل

أظهر التحليل المحلي والسجل البعيد لنفس SHA **177 ملاحظة معلوماتية فقط**؛ لم تظهر أي `warning` أو `error`. كان فشل بوابة الكود السابقة ناتجًا حصريًا عن تحويل مستوى `info` إلى فشل عبر `--fatal-infos`، وليس عن خطأ محلل من درجات التحذير أو الخطأ.

| الشدة | العدد | قرار البوابة |
|---|---:|---|
| `error` | 0 | تبقى مانعة للدمج. |
| `warning` | 0 | تبقى مانعة للدمج. |
| `info` | 177 | استشاري؛ يسجل العدد وتظهر زيادة فوق خط الأساس كتحذير مرئي. |

يوضح الجدول التالي توزيع الدين المعلوماتي. لا تعطّل أي قاعدة lint ولا تضاف استثناءات عامة؛ يقتصر التغيير على إعادة دلالة الشدة التي يقررها المحلل أصلًا.

| قاعدة التحليل | العدد | التصنيف التشغيلي |
|---|---:|---|
| `require_trailing_commas` | 57 | تنسيق/اتساق. |
| `directives_ordering` | 27 | تنظيم الاستيراد. |
| `prefer_const_constructors` | 20 | تحسين أسلوب/أداء محلي. |
| `avoid_redundant_argument_values` | 20 | تبسيط أسلوبي. |
| `prefer_expression_function_bodies` | 14 | تبسيط أسلوبي. |
| `always_put_required_named_parameters_first` | 13 | اتساق واجهات. |
| `prefer_const_literals_to_create_immutables` | 8 | تحسين أسلوب/أداء محلي. |
| `comment_references` | 4 | دقة التوثيق داخل الكود. |
| `unnecessary_library_directive` | 3 | تنظيف أسلوبي. |
| `prefer_int_literals` | 2 | تبسيط أسلوبي. |
| `omit_local_variable_types` | 2 | تبسيط أسلوبي. |
| القواعد ذات الحالة المنفردة | 7 | مراجعة مركزة في دفعات لاحقة. |

تتضمن الحالات المنفردة `discarded_futures` و`cast_nullable_to_non_nullable` إلى جانب قواعد ترتيب أو تبسيط أخرى. ستراجع الحالتان الأوليان قبل أي دفعة تنسيق جماعية لأنهما قد تستحقان قرارًا دلاليًا خاصًا؛ لا يجري تعديلهما آليًا لمجرد خفض العدد.

## نتائج بوابة الاختبارات

أكمل السجل البعيد لبوابة الاختبارات `1,212` اختبارًا مع `2` متخطيين، لكنه أنهى خطوة `Run tests with coverage` برمز خروج `1`؛ ولذلك تخطت البوابة فحص التغطية التالي. في المقابل، أعاد التنفيذ المحلي للأمر نفسه على SHA المذكور تشغيلًا ناجحًا وإنتاج `coverage/lcov.info`. لا يكفي ذلك لإثبات أن المشكلة عابرة أو أن الاختبارات مستقرة في كل بيئة.

| الدليل | النتيجة | القرار |
|---|---|---|
| تنفيذ CI البعيد `Run tests with coverage` | فشل برمز خروج `1` بعد ملخص النجاح الظاهر. | تبقى بوابة الاختبارات مانعة؛ لا تخفّض عتبتها ولا تتجاوزها. |
| السجل البعيد | ظهر إخراج من finalizer خاص بجسر ZATCA حول `MissingPluginException`، من دون فشل assertion ظاهر في الملخص. | يسجل كمؤشر تشخيصي فقط، لا كسبب مثبت حتى يعاد إنتاجه. |
| `flutter test --coverage` محليًا | نجح على نفس SHA وأنتج ملف التغطية. | دليل قابل لإعادة الإنتاج محليًا، لكنه لا يلغي الدليل البعيد. |

## قرار مواءمة البوابة

سيستبدل تنفيذ بوابة التحليل `flutter analyze --fatal-infos` بـ`flutter analyze --no-fatal-infos`. تبقى أخطاء وتحذيرات المحلل مانعة للدمج، وتسجل الملاحظات المعلوماتية في مخرج CI مع خط أساس `177`. ستصدر البوابة تحذيرًا إذا زاد العدد، من دون تحويل هذا التحذير إلى فشل في هذه المرحلة.

> **لا يطبق هذا القرار أي `exclude` أو `ignore` أو تعطيل لقواعد lint، ولا يغير بوابة الاختبارات أو عتبة التغطية.**

| مرحلة التدرج | شرط الانتقال | إجراء مقترح |
|---|---|---|
| الآن | خط أساس موثق: 0 أخطاء، 0 تحذيرات، 177 ملاحظة معلوماتية. | أخطاء/تحذيرات مانعة، والمعلوماتية استشارية مع تحذير عند النمو. |
| بعد معالجة ملاحظات الدلالة | مراجعة صريحة لـ`discarded_futures` و`cast_nullable_to_non_nullable` مع اختبار أو قرار موثق لكل تغيير. | جعل القواعد المختارة مانعة إن لزم. |
| بعد دفعات التنسيق | انخفاض ثابت في العدد خلال دورتين من CI من دون زيادة غير مفسرة. | تخفيض السقف الاستشاري تدريجيًا بدل فرض الصفر دفعة واحدة. |
| قبل أي ادعاء تحسن نهائي | SHA جديد، سجل CI أخضر، ومقارنة خط أساس محدثة. | تحديث سجل أدلة جديد؛ لا تعديل هذه اللقطة التاريخية. |

## مراجع

[1]: [خارطة تنفيذ BKIP](EXECUTION_ROADMAP.md) — سياسة التدرج من الوضع الاستشاري إلى البوابات المانعة.
[2]: [تعريف بوابات الجودة](../../../.github/workflows/quality_gates.yml) — أوامر CI قبل تعديل الدفعة.

**المؤلف:** Manus AI
